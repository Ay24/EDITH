# Cloud-mode additional dependencies
# These are installed automatically by launch_cloud.py if missing.
# Run manually: pip install -r requirements_cloud.txt

groq>=0.9.0
edge-tts>=6.1.9
duckduckgo-search>=6.1.0
